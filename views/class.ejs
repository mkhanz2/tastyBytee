<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Baking Class Registration</title>
    <link href="https://fonts.googleapis.com/css2?family=Baloo+2&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
       margin: 0;
        padding: 0;
        font-family: 'Baloo 2';
        font-weight: 400;
        font-style: normal;
      background-color: #fffaf5; /* Soft cream baking theme */
      color: #333;
      line-height: 1.6;
      padding: 20px;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      background: white;
      padding: 40px;
      border-radius: 16px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
    }

    .title {
      font-family: 'Playfair Display', serif;
      font-size: 2.5rem;
      text-align: center;
      margin-bottom: 20px;
      color: green; /* Soft brownish pink */
    }

    .hero-img {
      width: 100%;
      border-radius: 10px;
      margin-bottom: 30px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.05);
    }

    .section {
      margin-bottom: 30px;
    }

    .section h2 {
      font-size: 1.5rem;
      margin-bottom: 15px;
      text-align: center;
      color: green;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    input[type="text"],
    input[type="email"],
    input[type="date"] {
      padding: 10px 15px;
      border: 1px solid #ddd;
      border-radius: 8px;
      font-size: 1rem;
      width: 100%;
      transition: border-color 0.3s;
    }

    input:focus {
      border-color: green;
      outline: none;
    }

    .batch-options {
      background: #d0e3cc;
      padding: 15px;
      border-radius: 10px;
      border: 1px solid #d3f3d0;
    }

    .batch-options h3 {
      margin-bottom: 10px;
      color: green;
    }

    label {
      display: block;
      margin-bottom: 8px;
      cursor: pointer;
    }

    button {
      cursor: pointer;
      padding: 12px;
      background-color: green;
      color: white;
      border-radius: 8px;
      border: none;
      font-size: 1.1rem;
      transition: all 0.3s;
    }

    button:hover {
      background-color: rgb(54, 164, 54);
      transform: scale(1.05);
    }

    @media (max-width: 600px) {
      .title {
        font-size: 2rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="title">Join Our Baking Classes</h1>
    <img src="/images/asset 1.png" alt="Baking Class" class="hero-img">

    <div class="section">
      <h2>Fill the Registration Form</h2>
      <form action="/join-classes" method="POST">
        <input type="text" name="name" placeholder="<%= user.name %>" readonly>
        <input type="email" name="email" value="<%= user.email %>" placeholder="<%= user.email %>" readonly>
        <input type="text" name="address" value="<%= user.address %>" placeholder="<%= user.address %>" readonly>

        <div class="batch-options">
          <h3>Select Your Batch</h3>
          <label>
            <input type="radio" name="batch" value="morning" required>
            Morning Batch (9:00 AM – 11:00 AM)
          </label>
          <label>
            <input type="radio" name="batch" value="afternoon">
            Afternoon Batch (1:00 PM – 3:00 PM)
          </label>
          <label>
            <input type="radio" name="batch" value="evening">
            Evening Batch (5:00 PM – 7:00 PM)
          </label>
        </div>
        <button type="button" onclick="window.location.href='/account-details'">Update information</button>
        <button type="submit">Submit</button>
      </form>
    </div>
  </div>
</body>
</html>
